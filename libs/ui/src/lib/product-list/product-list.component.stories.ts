import { ProductListComponent } from './product-list.component';

export default {
  title: 'ProductListComponent',
};

export const primary = () => ({
  moduleMetadata: {
    imports: [],
  },
  component: ProductListComponent,
  props: {},
});
